Don't change or add file in tests folder, if you want to write test add it to unit_tests folder.

## Run code
Run any code in the repo using
```sh
uv run <python_file_path>
```

### Run unit tests

valid result

```sh
uv run pytest
```

